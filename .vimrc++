" General
syntax on
set number
set tabstop=4
set autoindent
set incsearch
set hlsearch
set shiftwidth=4
set expandtab
set mouse=a
set cursorline
set colorcolumn=110

" Comment this line if powerline is not installed
source ~/.vimrc_powerline

" Comment this line if vundle is not installed
source ~/.vimrc_vundle
